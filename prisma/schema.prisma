generator photon {
  provider = "photonjs"
}

generator nexus_prisma {
  provider = "nexus-prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:prisma/dev.db"
}

model User {
  id       String  @default(cuid()) @id
  email    String  @unique
  password String
  name     String?
}
