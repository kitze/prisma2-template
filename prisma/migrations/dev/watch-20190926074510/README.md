# Migration `watch-20190926074510`

This migration has been generated by Kristijan Ristovski at 9/26/2019, 7:45:10 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "lift"."User" ADD COLUMN "surname" TEXT    ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20190926074405..watch-20190926074510
--- datamodel.dml
+++ datamodel.dml
@@ -26,5 +26,6 @@
   email    String  @unique
   password String
   name     String?
   posts    Post[]
-}
+  surname String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190926074510)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190926074510'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
