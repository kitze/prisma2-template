# Migration `watch-20190926112217`

This migration has been generated by Kristijan Ristovski at 9/26/2019, 11:22:17 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT    ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20190926112009..watch-20190926112217
--- datamodel.dml
+++ datamodel.dml
@@ -7,9 +7,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url      = "file:dev.db"
+  url      = "file:prisma/dev.db"
 }
 model User {
   id       String  @default(cuid()) @id
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190926112217)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190926112217'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
